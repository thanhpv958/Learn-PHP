<?php
    /*$fo = @fopen('./01-base.php', 'r'); // đặt @ phía trước hàm fopen để k bug lỗi ra màn hình

    if($fo) {
        // ĐỌC CẢ FILE
        // $data = fread($fo, filesize('./03-formdemo.html'));
        // echo $data;

        // ĐỌC TỪ KÍ TỰ
        // dùng while và hàm feof để lặp qua từng kí tự
        while(!feof($fo)){
            echo fgetc($fo);
        }

        // ĐỌC single line (từng dòng) dùng hàm fgets();
    }
    else {
        echo "error";
    } 

    echo file_get_contents('http://thanhphan.tk'); // Lấy nội dung của 1 file hoặc 1 website
    */

  
    // copy('./02-func.php', './02-functest.php');

?>